# Required dependencies
# The application needs these to run at all (app.py imports them unconditionally):

streamlit>=1.50.0  # Web UI framework (app.py)
pandas>=2.0.0  # Data tables and CSV export

# Optional dependencies
# Install these for enhanced features:

loguru>=0.7.0  # Enhanced logging with rotation (optional but recommended)
pillow>=10.0.0  # For reading photo EXIF metadata
